export { default } from './Details';

// import { connect } from 'react-redux';

// import Details from './Details';

// import { handleAllAds } from '../../store/action';
// import { getStateAds } from '../../store/selectors';

// function mapStateToProps(state, ownProps) {
//     return {
//         getAllAds: getStateAds(state),
//     }
// }

// function mapDispatchToProps(dispatch, ownProps) {
//     return {
//         handleAllAds: () => handleAllAds(dispatch),
//     }
// }

// const connected = connect(mapStateToProps, mapDispatchToProps);
// const DetailsConnected = connected(Details);

// export default DetailsConnected;